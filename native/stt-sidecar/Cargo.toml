[package]
name = "flapstack-stt-sidecar"
version = "0.1.0"
edition = "2021"
license = "MIT"

[dependencies]
base64 = "0.22"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
transcribe-cpp = { version = "=0.1.3" }

[profile.release]
lto = "thin"
strip = true
