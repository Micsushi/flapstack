# Mobile bridge headless fixtures

These fixtures drive mocked lifecycle tests only. They model approved, changed,
and public network interfaces plus accepted and rejected Host/Origin headers.

The suite never opens a real listener or performs browser, device, UI, package,
or live-network verification. Those observations remain manual acceptance work.
